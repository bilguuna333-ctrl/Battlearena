kind = "web"
previewPath = "/"
title = "CodeSteppe"
version = "1.0.0"
id = "artifacts/battle-arena"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18292

[services.development]
run = "pnpm --filter @workspace/battle-arena run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/battle-arena", "run", "build" ]
publicDir = "artifacts/battle-arena/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18292"
BASE_PATH = "/"
